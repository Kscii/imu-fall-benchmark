schema_version: 2
id: all_temporal_fold0_pilot_v1
contract_path: configs/contracts/imu_benchmark_contract_v2.json
snapshot_path: active.json
data_view_path: configs/data_views/all_temporal_v1.yaml
model_catalog_path: configs/models/fall_models_v1.yaml
models:
  - threshold_impact
  - cuml_logistic_regression
  - cuml_random_forest
  - xgboost_cuda
  - torch_1d_cnn
  - torch_lstm
  - torch_cnn_lstm
folds: [0]
seeds: [3888]
precision: fp32
gpu_mode: auto
max_epochs: 50
patience: 8
max_sequences_per_split: null
max_windows_per_sequence: null
cache_flush_windows: 16384
data_quality_status: internal_fold0_pilot
known_limitations:
  - This single-fold pilot is engineering evidence, not a final multi-fold comparison.
  - Public temporal datasets are evaluated by participant fold; team data is training-only.
  - Window false positives per hour are not product alarm episodes per hour.
